# 技术手册 - 米家设备控制台

## 项目概述
基于 Flask + FastAPI 构建的米家智能家居设备控制程序，提供 Web 界面控制小米设备。
- 后端：FastAPI（主应用）+ Flask（设备控制 API 子应用）
- 前端：Tailwind CSS + 原生 JavaScript
- 数据库：SQLite（日志存储，两周自动清理）
- 配置：.env 文件

## 项目结构
```
mi-control/
├── .env.example          # 环境变量示例（复制为.env后填写）
├── .gitignore
├── requirements.txt      # Python依赖
├── config.py             # 配置加载模块
├── database.py           # SQLite数据库管理
├── auth_manager.py       # 鉴权管理（登录/锁定/会话）
├── flask_app.py          # Flask子应用（设备控制API）
├── main.py               # FastAPI主应用入口
├── mi_lib/               # 米家API核心库
│   ├── __init__.py
│   ├── account.py        # 小米账号登录（密码登录+二次验证处理）
│   ├── qr_login.py       # 小米账号二维码登录（扫码登录）
│   ├── service.py        # 设备服务（列表/控制）
│   ├── spec.py           # 设备规格获取（从home.miot-spec.com解析spec）
│   └── utils.py          # 工具函数（签名/哈希）
├── static/
│   ├── css/style.css     # 自定义样式
│   └── js/app.js         # 前端交互逻辑
└── templates/
    ├── login.html        # 登录页面
    └── index.html        # 控制台主页
```

## 配置说明
复制 `.env.example` 为 `.env`，填写以下配置：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| MI_USERNAME | 小米账号 | 空（必填） |
| MI_PASSWORD | 小米密码 | 空（必填） |
| MI_NICKNAME | 昵称 | 空 |
| ADMIN_PASSWORD | 控制台登录密码 | admin123 |
| SECRET_KEY | 应用密钥 | 需修改 |
| DATABASE_PATH | 数据库路径 | ./mi_control.db |
| HOST | 监听地址 | 0.0.0.0 |
| PORT | 监听端口 | 8000 |

## 启动方式
```bash
cd mi-control
pip install -r requirements.txt
python main.py
```
访问 http://localhost:8000

---

## 模块详解

### config.py - 配置加载模块
从 `.env` 文件加载所有配置项，使用 `python-dotenv` 库。
- 输入：`.env` 文件
- 输出：全局配置常量
- 复用方式：`from config import MI_USERNAME, ADMIN_PASSWORD, PORT`

### database.py - 数据库管理模块
使用 SQLite 存储日志和持久化数据，线程安全（threading.Lock）。

**数据表：**
- `operation_logs` - 操作日志（设备操作记录）
- `connection_logs` - 连接日志（登录/登出记录）
- `settings` - 设置项（键值对持久化存储）

**核心函数：**
| 函数 | 输入 | 输出 | 说明 |
|------|------|------|------|
| init_db() | 无 | 无 | 初始化数据库表 |
| log_operation(user, action, device_id, device_name, params, result, success) | 操作信息 | 无 | 记录设备操作日志 |
| log_connection(ip, user_agent, action, username, success) | 连接信息 | 无 | 记录连接日志 |
| cleanup_old_logs() | 无 | 无 | 清理14天前的日志 |
| get_operation_logs(limit) | 条数 | list | 查询操作日志 |
| get_connection_logs(limit) | 条数 | list | 查询连接日志 |
| set_setting(key, value) | 键值对 | 无 | 保存设置项 |
| get_setting(key, default) | 键名 | str | 读取设置项 |

### auth_manager.py - 鉴权管理模块
负责登录验证、失败锁定、会话管理。

**核心函数：**
| 函数 | 输入 | 输出 | 说明 |
|------|------|------|------|
| check_login(ip, password) | IP, 密码 | (bool, str, token) | 登录验证，5次失败锁定60秒 |
| verify_session(token) | Token | bool | 验证会话有效性（15分钟超时） |
| touch_session(token) | Token | 无 | 刷新会话活动时间 |
| logout(token) | Token | 无 | 注销会话 |
| get_user(token) | Token | str | 获取会话用户名 |

**鉴权逻辑：**
1. 连续5次密码错误 → 锁定IP 60秒
2. 锁定期间任何登录尝试 → 返回锁定提示
3. 登录成功 → 清除失败记录，生成Token
4. 每次API请求 → 验证Token + 更新活动时间
5. 15分钟无操作 → 会话过期

### mi_lib/ - 米家API核心库
基于 simple-mi-home 项目适配，使用 .env 替代 config.ini。

**mi_lib/utils.py - 工具函数**
| 函数 | 输入 | 输出 | 说明 |
|------|------|------|------|
| get_random(length) | 长度 | str | 生成随机字符串 |
| generate_nonce() | 无 | str | 生成请求nonce |
| generate_signed_nonce(secret, nonce) | 密钥, nonce | str | 生成签名nonce |
| generate_signature(url, signed_nonce, nonce, data) | 签名参数 | str | 生成请求签名 |
| sign_data(uri, data, secret) | 接口路径, 数据, 密钥 | dict | 组装请求参数 |
| get_hash(password) | 密码 | str | MD5哈希（小米协议要求） |

**mi_lib/account.py - 账号登录**
| 类 | 说明 |
|------|------|
| TokenStore | Token持久化存储（.mi_token.json） |
| MiAccountSession | 小米账号会话（登录/Token管理） |
| NeedVerifyError | 二次验证异常（携带验证URL） |

account.py 关键方法：
| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| _login() | 无 | dict | 执行密码登录，触发二次验证时抛出NeedVerifyError |
| _init_session() | 无 | Session | 初始化会话，检查token完整性 |
| _complete_login() | 登录响应 | dict | 完成登录获取serviceToken |

**mi_lib/qr_login.py - 二维码登录**
| 类 | 说明 |
|------|------|
| MiQrLogin | 二维码登录器（生成二维码/长轮询/获取Token） |

qr_login.py 关键方法：
| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| generate_qr() | 无 | dict | 生成二维码，返回{ticket, qr_image_url, poll_url, timeout} |
| fetch_qr_image(qr_image_url) | 图片URL | bytes | 获取二维码图片字节流（用于代理转发） |
| poll_once(poll_url) | 轮询URL | dict | 长轮询扫码状态，返回{status, message, token} |
| _get_service_token(location, nonce, ssecurity) | 跳转URL, nonce, 密钥 | str | 用SHA1签名换取serviceToken |

二维码登录流程（基于小米 longPolling 接口）：
1. GET /longPolling/loginUrl 获取二维码图片URL和长轮询URL
2. 用户用米家APP扫码确认
3. 长轮询 lp URL 等待登录成功
4. 获取 location，用 SHA1(nonce+ssecurity) 生成 clientSign 换取 serviceToken

状态常量：
| 常量 | 值 | 说明 |
|------|-----|------|
| QR_STATUS_WAITING | waiting | 等待扫码 |
| QR_STATUS_SCANNED | scanned | 已扫码未确认 |
| QR_STATUS_CONFIRMED | confirmed | 已确认登录成功 |
| QR_STATUS_EXPIRED | expired | 二维码过期 |
| QR_STATUS_CANCELED | canceled | 用户取消 |
| QR_STATUS_FAILED | failed | 登录失败 |

**mi_lib/service.py - 设备服务**
| 类/函数 | 说明 |
|------|------|
| Device | 设备控制（属性读写、动作执行） |
| MiService | 设备服务（列表获取、设备查找） |

Device 方法：
| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| do_action(sid, aid, values, pid) | 服务ID, 动作ID, 参数 | dict | 执行设备动作 |
| get_device_prop(sid, pid) | 服务ID, 属性ID | dict | 获取单个属性 |
| get_device_props(items) | 属性列表 | dict | 批量获取属性 |
| set_device_prop(sid, pid, value) | 服务ID, 属性ID, 值 | dict | 设置单个属性 |
| set_device_props(items) | 属性列表 | dict | 批量设置属性 |

MiService 方法：
| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| get_device_list() | 无 | dict | 获取设备列表 |
| find_device(device_name) | 设备名称 | Device | 按名称查找设备 |
| use_device(device_id) | 设备ID | Device | 按ID获取设备控制实例 |

**mi_lib/spec.py - 设备规格获取模块**
从 `home.miot-spec.com` 抓取并解析设备 spec 定义，用于动态生成控制 UI（替代旧版固定 Tab：属性查询/属性设置/动作执行）。

抓取流程：
1. 访问 `https://home.miot-spec.com/spec?type={model}` 获取 HTML 页面
2. 通过字符串括号匹配从 HTML 中提取内嵌的 `tree` JSON（services/properties/actions）
3. 提取 `i18n` JSON，应用 `zh_cn` 中文翻译
4. 用正则提取 URN
5. 构建结果并写入内存缓存（TTL 24小时）

**i18n key 拼接规则**（关键）：
tree 中 service 和 valueList 项自带 `i18nKey` 字段，但 property 和 action **没有** i18nKey，需按 iid 拼接：
- service：`service:{siid:03d}`（如 `service:002`）
- property：`service:{siid:03d}:property:{piid:03d}`（如 `service:002:property:001`）
- action：`service:{siid:03d}:action:{aiid:03d}`（如 `service:002:action:001`）
- valueList 项：直接用自带的 `i18nKey` 字段

**valueRange 数据格式**：
spec 中 `valueRange` 是数组 `[min, max, step]`（如 `[0, 480, 1]`），前端需用 `Array.isArray()` 判断后取值。

| 函数 | 输入 | 输出 | 说明 |
|------|------|------|------|
| get_device_spec(model, lang) | 型号, 语言 | dict | 获取设备规格（带24小时缓存） |
| build_control_items(spec) | spec字典 | list | 过滤siid=1设备信息服务，构建前端控制项 |

`build_control_items` 输出结构：
```json
[{
  "siid": 2,
  "service_name": "风扇",
  "properties": [{"piid": 1, "name": "开关", "format": "bool", "access": ["read","write"], ...}],
  "actions": [{"aiid": 1, "name": "开关状态切换", "in": [], "out": []}]
}]
```

复用方式：
```python
from mi_lib.spec import get_device_spec, build_control_items
spec = get_device_spec("dmaker.fan.p8")
items = build_control_items(spec)
```

### flask_app.py - Flask子应用
通过 WSGI 中间件挂载到 FastAPI，路径前缀 `/miapi`。

**API 端点：**
| 路径 | 方法 | 鉴权 | 说明 |
|------|------|------|------|
| /miapi/get_device_list | GET | 是 | 获取设备列表 |
| /miapi/do_action | POST | 是 | 执行设备动作 |
| /miapi/get_prop | POST | 是 | 获取单个属性 |
| /miapi/get_props | POST | 是 | 批量获取属性 |
| /miapi/set_prop | POST | 是 | 设置单个属性 |
| /miapi/set_props | POST | 是 | 批量设置属性 |
| /miapi/get_spec | POST | 是 | 获取设备规格（按model从home.miot-spec.com解析） |
| /miapi/qr/generate | POST | 否 | 生成二维码登录 |
| /miapi/qr/image | GET | 否 | 代理获取二维码图片 |
| /miapi/qr/poll | POST | 否 | 长轮询扫码状态 |

设备控制接口需 `Authorization: Bearer <token>` 头；二维码登录接口无需鉴权。

二维码登录接口说明：
- `/miapi/qr/generate`：生成二维码，返回 {ticket, qr_image_url}，qr_image_url 为代理路径
- `/miapi/qr/image?ticket=xxx`：代理获取二维码 PNG 图片（避免浏览器跨域）
- `/miapi/qr/poll`：长轮询扫码状态，输入 {ticket}，返回 {status, message}

### main.py - FastAPI主应用
提供 Web 界面和鉴权接口，挂载 Flask 子应用。

**路由：**
| 路径 | 方法 | 说明 |
|------|------|------|
| / | GET | 登录页面 |
| /dashboard | GET | 控制台主页 |
| /api/auth/login | POST | 登录 |
| /api/auth/logout | POST | 登出 |
| /api/auth/check | GET | 检查会话 |
| /api/auth/touch | POST | 刷新会话 |
| /api/settings | GET | 读取系统设置 |
| /api/settings | POST | 保存系统设置 |
| /api/logs/operation | GET | 查询操作日志 |
| /api/logs/connection | GET | 查询连接日志 |
| /api/status | GET | 服务状态 |

### static/js/app.js - 前端交互
功能模块：
- 鉴权检查（页面加载时验证Token）
- 实时时钟（北京时间，每秒更新）
- 设备列表加载和卡片渲染（含分页）
  - 单页卡片数由 `appSettings.pageLimit` 控制（4-48，默认12）
  - 超出部分通过"上一页/下一页"按钮切换
  - 设备列表缓存到 `cachedDevices`，翻页不重新请求
- 页面切换（设备列表 / 设置页）
  - 左侧栏两个按钮：设备（默认）、设置
  - 切换时高亮当前按钮，隐藏另一页面
- 设备控制弹窗（基于spec动态生成控件）
  - 打开弹窗时调用 `/miapi/get_spec` 拉取设备规格
  - 按属性 format/access/valueList/valueRange 自动渲染对应控件：
    - `bool` + write → 开关按钮（乐观更新+失败回滚）
    - `valueList` + write → 下拉选择框
    - `valueRange` + 数值格式 → 滑块（支持数组 `[min,max,step]` 和字符串格式，带实时数值显示）
    - 数值/字符串 + write（无 valueRange/valueList）→ 输入框
    - 其它 → 只读文本（自动显示当前值）
  - 弹窗打开后自动调用 `/miapi/get_props` 批量加载属性当前值
  - 服务分区展示，每个服务下的动作以按钮组形式呈现
  - 底部"刷新状态"按钮可重新拉取属性值
  - 错误处理分离：网络错误/API错误/渲染错误分别提示
- 系统设置（持久化到 SQLite settings 表）
  - 颜色模式：日间/夜间（body.dark-mode 类切换，夜间样式定义在 style.css）
  - 透明度：卡片/左栏/顶栏（30-100%，动态注入 `<style>` 调整 opacity）
  - 背景图片：支持 PNG/JPEG/GIF/WebP/BMP，以 dataURL 存 localStorage（≤5MB），支持背景模糊（0-20px）和遮罩（0-80%）
  - 单页卡片数：4-48，实时重新分页
  - 设置变更实时应用 + 500ms 防抖保存到服务器
  - "恢复默认"一键重置
- 空闲检测（15分钟无操作自动登出）
- 会话保活（每5分钟向服务器发送保活请求）
- Toast通知系统

---

## API 响应格式
统一格式：
```json
{
    "code": 0,        // 0=成功，非0=失败
    "message": "...",  // 提示信息
    "data": {}         // 数据
}
```

## 部署说明
### Windows
```bash
cd mi-control
pip install -r requirements.txt
copy .env.example .env
# 编辑 .env 填写小米账号
python main.py
```

### Linux
```bash
cd mi-control
pip install -r requirements.txt
cp .env.example .env
# 编辑 .env 填写小米账号
python3 main.py
```
